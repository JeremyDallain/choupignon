<nav class="navbar navbar-expand-sm navbar-dark bg-success">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('home') }}">Choupignon</a>
		<button class="navbar-toggler ms-auto" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link {{ (app.request.attributes.get('_route') == 'app_logout') ? 'active' : '' }}" aria-current="page" href="{{ path('app_logout') }}">Se déconnecter</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link {{ (app.request.attributes.get('_route') == 'app_register') ? 'active' : '' }}" aria-current="page" href="{{ path('app_register') }}">S'inscrire</a>
					</li>
					<li class="nav-item">
						<a class="nav-link {{ (app.request.attributes.get('_route') == 'app_login') ? 'active' : '' }}" aria-current="page" href="{{ path('app_login') }}">Se connecter</a>
					</li>
				{% endif %}</ul>
				<br>
				<ul class="navbar-nav ms-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a class="nav-link {{ (app.request.attributes.get('_route') == 'home') ? 'active' : '' }}" aria-current="page" href="{{ path('home') }}">Accueil</a>
				</li>
				<li class="nav-item">
					<a class="nav-link {{ (app.request.attributes.get('_route') == 'item_index') ? 'active' : '' }}" aria-current="page" href="{{ path('item_index') }}">Items</a>
				</li>
				<li class="nav-item">
					<a class="nav-link {{ (app.request.attributes.get('_route') == 'creator_new') ? 'active' : '' }}" aria-current="page" href="{{ path('creator_new') }}">New</a>
				</li>
			</ul>
		</div>
	</div>
</nav>
