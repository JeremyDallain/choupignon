{% for type, messages in app.flashes %}
    <div class="position-fixed top-0 top-alert alert alert-{{ type }} text-center">
        {% for message in messages %}
            <div>{{ message }}</div>
        {% endfor %}				
    </div>
{% endfor %}